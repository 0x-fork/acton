# tree-sitter-func

Tree-sitter grammar for the FunC language used by TON Language Server.

The grammar source originates from the FunC implementation in
`ton-blockchain/ton-language-server`. Generated parser files are kept in this
crate so native and WebAssembly builds use the same grammar.
