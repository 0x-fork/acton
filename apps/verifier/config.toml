[server]
bind_addr = "127.0.0.1:3000"

[network]
name = "mainnet"

[toncenter]

[source_repository]
remote = "origin"
author_name = "ton-verifier"
author_email = "ton-verifier@example.invalid"

[registry_index]
path = "verifier-index.sqlite3"

[compiler]
node_bin = "node"
worker_path = "compiler-worker/compile.mjs"
timeout_ms = 5000
